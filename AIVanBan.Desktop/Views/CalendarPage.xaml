<Page x:Class="AIVanBan.Desktop.Views.CalendarPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      Title="Lịch tổng hợp"
      Background="#F0F2F5">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="400"/>
        </Grid.ColumnDefinitions>

        <!-- ═══ LEFT: LỊCH ═══ -->
        <DockPanel Grid.Column="0" Margin="20">
            
            <!-- HEADER: Điều hướng tháng -->
            <materialDesign:Card DockPanel.Dock="Top" Padding="20,14" Margin="0,0,0,16"
                                 materialDesign:ElevationAssist.Elevation="Dp2">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Style="{StaticResource MaterialDesignIconButton}"
                                Width="40" Height="40" Click="PrevMonth_Click" ToolTip="Tháng trước">
                            <materialDesign:PackIcon Kind="ChevronLeft" Width="28" Height="28"/>
                        </Button>
                        <Button Style="{StaticResource MaterialDesignIconButton}"
                                Width="40" Height="40" Click="NextMonth_Click" ToolTip="Tháng sau">
                            <materialDesign:PackIcon Kind="ChevronRight" Width="28" Height="28"/>
                        </Button>
                        <Button Style="{StaticResource MaterialDesignFlatButton}"
                                Height="36" Click="Today_Click" Margin="10,0,0,0"
                                Content="📍 Hôm nay" FontSize="14" FontWeight="Medium"/>
                    </StackPanel>
                    
                    <TextBlock x:Name="txtMonthYear" Grid.Column="1"
                               Text="Tháng 01/2025" FontSize="24" FontWeight="Bold"
                               Foreground="#1A237E" HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                    
                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Background="#FFCDD2" CornerRadius="6" Padding="10,5" Margin="0,0,8,0">
                            <TextBlock Text="🔴 Quá hạn" FontSize="13" FontWeight="Medium" Foreground="#C62828"/>
                        </Border>
                        <Border Background="#FFF9C4" CornerRadius="6" Padding="10,5" Margin="0,0,8,0">
                            <TextBlock Text="🟡 Sắp hạn" FontSize="13" FontWeight="Medium" Foreground="#F57F17"/>
                        </Border>
                        <Border Background="#BBDEFB" CornerRadius="6" Padding="10,5" Margin="0,0,8,0">
                            <TextBlock Text="🔵 Họp" FontSize="13" FontWeight="Medium" Foreground="#1565C0"/>
                        </Border>
                        <Border Background="#C8E6C9" CornerRadius="6" Padding="10,5">
                            <TextBlock Text="🟢 Nhiệm vụ" FontSize="13" FontWeight="Medium" Foreground="#2E7D32"/>
                        </Border>
                    </StackPanel>
                </Grid>
            </materialDesign:Card>
            
            <!-- CALENDAR GRID -->
            <materialDesign:Card Padding="12" materialDesign:ElevationAssist.Elevation="Dp2">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="44"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Tiêu đề ngày trong tuần -->
                    <UniformGrid Grid.Row="0" Columns="7">
                        <TextBlock Text="Thứ 2" FontWeight="Bold" FontSize="14" Foreground="#37474F" 
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <TextBlock Text="Thứ 3" FontWeight="Bold" FontSize="14" Foreground="#37474F"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <TextBlock Text="Thứ 4" FontWeight="Bold" FontSize="14" Foreground="#37474F"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <TextBlock Text="Thứ 5" FontWeight="Bold" FontSize="14" Foreground="#37474F"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <TextBlock Text="Thứ 6" FontWeight="Bold" FontSize="14" Foreground="#37474F"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <TextBlock Text="Thứ 7" FontWeight="Bold" FontSize="14" Foreground="#E65100"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <TextBlock Text="CN" FontWeight="Bold" FontSize="14" Foreground="#C62828"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </UniformGrid>
                    
                    <!-- Ô ngày -->
                    <UniformGrid x:Name="calendarGrid" Grid.Row="1" Columns="7" Rows="6"/>
                </Grid>
            </materialDesign:Card>
        </DockPanel>

        <!-- ═══ RIGHT: CHI TIẾT NGÀY ═══ -->
        <Border Grid.Column="1" Margin="0,20,20,20">
            <materialDesign:Card Padding="20" materialDesign:ElevationAssist.Elevation="Dp2">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Margin="0,0,0,16">
                        <TextBlock x:Name="txtSelectedDate" Text="Đang tải..." 
                                   FontSize="20" FontWeight="Bold" Foreground="#1A237E"/>
                        <TextBlock x:Name="txtEventSummary" Text="" FontSize="14" 
                                   Foreground="#666" Margin="0,6,0,0"/>
                    </StackPanel>
                    
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="eventListPanel">
                            
                            <!-- Placeholder khi chưa chọn ngày -->
                            <StackPanel x:Name="emptyEventState" HorizontalAlignment="Center" Margin="0,50,0,0">
                                <materialDesign:PackIcon Kind="CalendarBlankOutline" Width="64" Height="64"
                                                        HorizontalAlignment="Center" Foreground="#CCC"/>
                                <TextBlock Text="Chọn một ngày trên lịch&#x0a;để xem sự kiện"
                                          TextAlignment="Center" FontSize="15" Foreground="#999" Margin="0,12,0,0"
                                          LineHeight="24"/>
                            </StackPanel>
                            
                        </StackPanel>
                    </ScrollViewer>
                </DockPanel>
            </materialDesign:Card>
        </Border>
    </Grid>
</Page>
