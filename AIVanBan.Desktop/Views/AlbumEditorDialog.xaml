<Window x:Class="AIVanBan.Desktop.Views.AlbumEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Album Editor"
        Width="700" Height="600"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5"
        ResizeMode="CanResize">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <materialDesign:Card Grid.Row="0" Padding="20" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="ImageAlbum" Width="32" Height="32" Foreground="#2196F3" VerticalAlignment="Center"/>
                <TextBlock x:Name="txtHeader" 
                           Text="Tạo Album Mới" 
                           FontSize="20" 
                           FontWeight="Bold" 
                           VerticalAlignment="Center" 
                           Margin="10,0"/>
            </StackPanel>
        </materialDesign:Card>

        <!-- Form -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="20">
            <StackPanel>
                <!-- Tiêu đề -->
                <TextBlock Text="Tiêu đề album *" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <TextBox x:Name="txtTitle"
                         materialDesign:HintAssist.Hint="Ví dụ: Hội nghị cán bộ 2026"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         FontSize="14"
                         Margin="0,0,0,20"/>

                <!-- Mô tả -->
                <TextBlock Text="Mô tả album" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <TextBlock Text="(Có thể viết dài như bài báo, ghi chú chi tiết về album)" 
                           FontSize="11" 
                           Foreground="#999" 
                           Margin="0,0,0,8"/>
                <TextBox x:Name="txtDescription"
                         materialDesign:HintAssist.Hint="Nhập mô tả chi tiết về album..."
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         MinHeight="250"
                         MaxHeight="400"
                         FontSize="13"
                         Padding="10"
                         Margin="0,0,0,20"/>

                <!-- Ngày diễn ra -->
                <TextBlock Text="Ngày diễn ra (nếu liên quan đến sự kiện/cuộc họp)" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <DatePicker x:Name="dpEventDate"
                            materialDesign:HintAssist.Hint="Chọn ngày diễn ra..."
                            Style="{StaticResource MaterialDesignOutlinedDatePicker}"
                            FontSize="13"
                            Margin="0,0,0,20"
                            Width="250"
                            HorizontalAlignment="Left"/>

                <!-- Tags -->
                <TextBlock Text="Tags (cách nhau bởi dấu phẩy)" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <TextBox x:Name="txtTags"
                         materialDesign:HintAssist.Hint="Ví dụ: Hội nghị, Kiểm tra, Tuyên truyền"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         FontSize="13"
                         Margin="0,0,0,10"/>
                
                <TextBlock Text="💡 Mẹo: Tags giúp tìm kiếm và phân loại album dễ dàng hơn" 
                           FontSize="11" 
                           Foreground="#FF9800" 
                           TextWrapping="Wrap"
                           Margin="0,0,0,20"/>

                <!-- Hướng dẫn -->
                <Border Background="#E8F5E9" 
                        BorderBrush="#4CAF50" 
                        BorderThickness="1" 
                        CornerRadius="5" 
                        Padding="15"
                        Margin="0,10,0,0">
                    <StackPanel>
                        <TextBlock Text="📋 Hướng dẫn sử dụng:" FontWeight="Bold" Foreground="#2E7D32" Margin="0,0,0,10"/>
                        <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="#1B5E20">
                            <Run Text="• Tiêu đề: Đặt tên ngắn gọn, dễ nhớ"/><LineBreak/>
                            <Run Text="• Mô tả: Viết chi tiết nội dung, mục đích, thời gian, địa điểm..."/><LineBreak/>
                            <Run Text="• Tags: Dùng để phân loại và tìm kiếm nhanh"/><LineBreak/>
                            <Run Text="• Sau khi tạo album, bạn có thể upload ảnh vào"/>
                        </TextBlock>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Footer Buttons -->
        <materialDesign:Card Grid.Row="2" Padding="20">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Hủy"
                        Click="Cancel_Click"
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Width="100"
                        Margin="0,0,10,0"/>
                
                <Button x:Name="btnSave"
                        Content="Lưu"
                        Click="Save_Click"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Background="#4CAF50"
                        BorderBrush="#4CAF50"
                        Width="120"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</Window>
