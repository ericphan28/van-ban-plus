<Page x:Class="AIVanBan.Desktop.Views.AIGeneratorPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      Title="AI Tạo văn bản">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ═══ HEADER ═══ -->
        <TextBlock Grid.Row="0" Text="✨ AI TẠO VĂN BẢN TỰ ĐỘNG" 
                   FontSize="24" FontWeight="Bold" Margin="0,0,0,15"/>

        <!-- ═══ NÚT TẠO VĂN BẢN + HƯỚNG DẪN ═══ -->
        <materialDesign:Card Grid.Row="1" Padding="24" Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Mô tả -->
                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="Soạn thảo văn bản hành chính bằng AI" 
                               FontSize="18" FontWeight="SemiBold" Margin="0,0,0,6"/>
                    <TextBlock TextWrapping="Wrap" Foreground="Gray" FontSize="13">
                        <Run Text="Chọn loại văn bản → Nhập thông tin → AI tạo nội dung chuẩn Nghị định 30/2020/NĐ-CP"/>
                        <LineBreak/>
                        <Run Text="💡 Có 40 kịch bản mẫu sẵn cho các loại: Công văn, Quyết định, Báo cáo, Tờ trình, Kế hoạch, Thông báo, Nghị quyết..."/>
                    </TextBlock>
                </StackPanel>
                
                <!-- Nút tạo -->
                <Button Grid.Column="1" 
                        x:Name="btnNewDocument" 
                        Style="{StaticResource MaterialDesignRaisedDarkButton}"
                        Height="60" MinWidth="260"
                        Click="NewDocument_Click"
                        Margin="20,0,0,0">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="FileDocumentPlus" Width="28" Height="28" 
                                                VerticalAlignment="Center" Margin="0,0,12,0"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="📝 TẠO VĂN BẢN MỚI" FontSize="16" FontWeight="Bold"/>
                            <TextBlock Text="Mở AI Soạn thảo văn bản" FontSize="11" Opacity="0.8"/>
                        </StackPanel>
                    </StackPanel>
                </Button>
            </Grid>
        </materialDesign:Card>

        <!-- ═══ DANH SÁCH VĂN BẢN AI ĐÃ TẠO ═══ -->
        <materialDesign:Card Grid.Row="2" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header bảng -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                    <materialDesign:PackIcon Kind="History" Width="22" Height="22" 
                                            VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="Văn bản AI đã tạo gần đây" FontWeight="Bold" FontSize="16" 
                               VerticalAlignment="Center"/>
                    <Button x:Name="btnRefresh" 
                            Style="{StaticResource MaterialDesignIconButton}"
                            ToolTip="Làm mới danh sách"
                            Click="Refresh_Click"
                            Margin="10,0,0,0">
                        <materialDesign:PackIcon Kind="Refresh"/>
                    </Button>
                </StackPanel>

                <!-- Danh sách văn bản -->
                <DataGrid x:Name="dgRecentDocuments" Grid.Row="1"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          SelectionMode="Single"
                          HeadersVisibility="Column"
                          MouseDoubleClick="DocumentDoubleClick"
                          materialDesign:DataGridAssist.CellPadding="12 8"
                          materialDesign:DataGridAssist.ColumnHeaderPadding="12 8"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Tiêu đề" Binding="{Binding Title}" Width="*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="TextWrapping" Value="Wrap"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Loại" Binding="{Binding TypeDisplay}" Width="130"/>
                        <DataGridTextColumn Header="Cơ quan" Binding="{Binding Issuer}" Width="180">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="TextWrapping" Value="Wrap"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Ngày tạo" Binding="{Binding CreatedDate, StringFormat=dd/MM/yyyy HH:mm}" Width="140"/>
                        <DataGridTemplateColumn Header="Thao tác" Width="130">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Style="{StaticResource MaterialDesignIconForegroundButton}"
                                                ToolTip="Xuất Word"
                                                Tag="{Binding Id}"
                                                Click="ExportWord_Click"
                                                Margin="0,0,4,0">
                                            <materialDesign:PackIcon Kind="MicrosoftWord" Foreground="#2B579A"/>
                                        </Button>
                                        <Button Style="{StaticResource MaterialDesignIconForegroundButton}"
                                                ToolTip="Xóa"
                                                Tag="{Binding Id}"
                                                Click="DeleteDocument_Click">
                                            <materialDesign:PackIcon Kind="Delete" Foreground="#E53935"/>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Empty State -->
                <StackPanel x:Name="emptyState" Grid.Row="1" 
                            VerticalAlignment="Center" HorizontalAlignment="Center"
                            Visibility="Collapsed">
                    <materialDesign:PackIcon Kind="FileDocumentOutline" Width="60" Height="60" 
                                            HorizontalAlignment="Center" Foreground="LightGray"/>
                    <TextBlock Text="Chưa có văn bản AI nào" FontSize="16" Foreground="Gray" 
                               HorizontalAlignment="Center" Margin="0,10,0,5"/>
                    <TextBlock Text="Nhấn nút &quot;TẠO VĂN BẢN MỚI&quot; để bắt đầu" FontSize="13" 
                               Foreground="LightGray" HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </materialDesign:Card>
    </Grid>
</Page>
