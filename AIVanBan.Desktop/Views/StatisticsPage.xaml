<Page x:Class="AIVanBan.Desktop.Views.StatisticsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      Title="Báo cáo thống kê"
      Background="#F5F5F5">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24">

            <!-- HEADER -->
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel>
                    <TextBlock Text="📋 Báo cáo thống kê văn bản" FontSize="22" FontWeight="Bold" Foreground="#37474F"/>
                    <TextBlock x:Name="txtPeriodInfo" Text="" FontSize="12" Foreground="#888" Margin="0,4,0,0"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <ComboBox x:Name="cboPeriod" Width="160" Margin="0,0,12,0"
                              materialDesign:HintAssist.Hint="Kỳ báo cáo"
                              SelectionChanged="Period_Changed"
                              SelectedIndex="1">
                        <ComboBoxItem Content="Tuần này"/>
                        <ComboBoxItem Content="Tháng này"/>
                        <ComboBoxItem Content="Quý này"/>
                        <ComboBoxItem Content="Năm nay"/>
                        <ComboBoxItem Content="Tất cả"/>
                    </ComboBox>
                    
                    <Button Style="{StaticResource MaterialDesignOutlinedButton}" 
                            Height="36" Click="ExportCsv_Click" Margin="0,0,8,0"
                            ToolTip="Xuất danh sách VB theo kỳ ra CSV">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="FileExport" Width="18" Height="18" VerticalAlignment="Center" Margin="0,0,4,0"/>
                            <TextBlock Text="Xuất CSV" FontSize="12"/>
                        </StackPanel>
                    </Button>
                    
                    <Button Style="{StaticResource MaterialDesignRaisedButton}" 
                            Height="36" Click="ExportExcel_Click" Margin="0,0,8,0"
                            Background="#217346" BorderBrush="#217346" Foreground="White"
                            ToolTip="Xuất báo cáo thống kê tổng hợp ra Excel (.xlsx)">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="MicrosoftExcel" Width="18" Height="18" VerticalAlignment="Center" Margin="0,0,4,0"/>
                            <TextBlock Text="Xuất Excel" FontSize="12"/>
                        </StackPanel>
                    </Button>
                    
                    <Button Style="{StaticResource MaterialDesignRaisedSecondaryButton}" 
                            Height="36" Click="Refresh_Click">
                        <materialDesign:PackIcon Kind="Refresh"/>
                    </Button>
                </StackPanel>
            </Grid>
            
            <!-- ═══════════════════════════════════════════════════ -->
            <!-- PHẦN 1: SO SÁNH KỲ TRƯỚC — Chỉ có ở Thống kê    -->
            <!-- ═══════════════════════════════════════════════════ -->
            <materialDesign:Card Padding="20" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="📊 SO SÁNH VỚI KỲ TRƯỚC" FontSize="13" FontWeight="Bold" Foreground="#37474F" Margin="0,0,0,14"/>
                    
                    <UniformGrid Columns="4">
                        <!-- VB Đến: kỳ này vs kỳ trước -->
                        <Border BorderBrush="#E0E0E0" BorderThickness="0,0,1,0" Padding="0,0,16,0" Margin="0,0,8,0">
                            <StackPanel>
                                <TextBlock Text="VB Đến" FontSize="11" Foreground="#888" FontWeight="SemiBold"/>
                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                    <TextBlock x:Name="txtCompDen" Text="0" FontSize="24" FontWeight="Bold" Foreground="#E65100"/>
                                    <TextBlock x:Name="txtCompDenDelta" Text="" FontSize="12" Foreground="#888"
                                              VerticalAlignment="Bottom" Margin="8,0,0,4"/>
                                </StackPanel>
                                <TextBlock x:Name="txtCompDenPrev" Text="Kỳ trước: 0" FontSize="11" Foreground="#AAA"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- VB Đi -->
                        <Border BorderBrush="#E0E0E0" BorderThickness="0,0,1,0" Padding="0,0,16,0" Margin="0,0,8,0">
                            <StackPanel>
                                <TextBlock Text="VB Đi" FontSize="11" Foreground="#888" FontWeight="SemiBold"/>
                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                    <TextBlock x:Name="txtCompDi" Text="0" FontSize="24" FontWeight="Bold" Foreground="#2E7D32"/>
                                    <TextBlock x:Name="txtCompDiDelta" Text="" FontSize="12" Foreground="#888"
                                              VerticalAlignment="Bottom" Margin="8,0,0,4"/>
                                </StackPanel>
                                <TextBlock x:Name="txtCompDiPrev" Text="Kỳ trước: 0" FontSize="11" Foreground="#AAA"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- Nội bộ -->
                        <Border BorderBrush="#E0E0E0" BorderThickness="0,0,1,0" Padding="0,0,16,0" Margin="0,0,8,0">
                            <StackPanel>
                                <TextBlock Text="Nội bộ" FontSize="11" Foreground="#888" FontWeight="SemiBold"/>
                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                    <TextBlock x:Name="txtCompNB" Text="0" FontSize="24" FontWeight="Bold" Foreground="#6A1B9A"/>
                                    <TextBlock x:Name="txtCompNBDelta" Text="" FontSize="12" Foreground="#888"
                                              VerticalAlignment="Bottom" Margin="8,0,0,4"/>
                                </StackPanel>
                                <TextBlock x:Name="txtCompNBPrev" Text="Kỳ trước: 0" FontSize="11" Foreground="#AAA"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- Tổng -->
                        <StackPanel>
                            <TextBlock Text="Tổng cộng" FontSize="11" Foreground="#888" FontWeight="SemiBold"/>
                            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                <TextBlock x:Name="txtCompTotal" Text="0" FontSize="24" FontWeight="Bold" Foreground="#37474F"/>
                                <TextBlock x:Name="txtCompTotalDelta" Text="" FontSize="12" Foreground="#888"
                                          VerticalAlignment="Bottom" Margin="8,0,0,4"/>
                            </StackPanel>
                            <TextBlock x:Name="txtCompTotalPrev" Text="Kỳ trước: 0" FontSize="11" Foreground="#AAA"/>
                        </StackPanel>
                    </UniformGrid>
                </StackPanel>
            </materialDesign:Card>
            
            <!-- ═══════════════════════════════════════════════════ -->
            <!-- PHẦN 2: HIỆU SUẤT XỬ LÝ — Dashboard không có    -->
            <!-- ═══════════════════════════════════════════════════ -->
            <materialDesign:Card Padding="20" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="⏱️ HIỆU SUẤT XỬ LÝ VĂN BẢN ĐẾN" FontSize="13" FontWeight="Bold" Foreground="#37474F" Margin="0,0,0,14"/>
                    
                    <UniformGrid Columns="4">
                        <StackPanel Margin="0,0,12,0">
                            <TextBlock Text="Tổng VB có hạn" FontSize="11" Foreground="#888"/>
                            <TextBlock x:Name="txtPerfTotal" Text="0" FontSize="22" FontWeight="Bold" Foreground="#1565C0" Margin="0,2,0,0"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,12,0">
                            <TextBlock Text="✅ Đúng hạn" FontSize="11" Foreground="#888"/>
                            <TextBlock x:Name="txtPerfOnTime" Text="0" FontSize="22" FontWeight="Bold" Foreground="#2E7D32" Margin="0,2,0,0"/>
                            <TextBlock x:Name="txtPerfOnTimeRate" Text="- %" FontSize="12" Foreground="#2E7D32"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,12,0">
                            <TextBlock Text="⚠ Quá hạn" FontSize="11" Foreground="#888"/>
                            <TextBlock x:Name="txtPerfOverdue" Text="0" FontSize="22" FontWeight="Bold" Foreground="#C62828" Margin="0,2,0,0"/>
                            <TextBlock x:Name="txtPerfOverdueRate" Text="- %" FontSize="12" Foreground="#C62828"/>
                        </StackPanel>
                        <StackPanel>
                            <TextBlock Text="Trung bình ngày xử lý" FontSize="11" Foreground="#888"/>
                            <TextBlock x:Name="txtPerfAvgDays" Text="-" FontSize="22" FontWeight="Bold" Foreground="#FF8F00" Margin="0,2,0,0"/>
                            <TextBlock Text="ngày/VB" FontSize="12" Foreground="#FF8F00"/>
                        </StackPanel>
                    </UniformGrid>
                </StackPanel>
            </materialDesign:Card>
            
            <!-- ═══════════════════════════════════════════════════ -->
            <!-- PHẦN 3: BẢNG TỔNG HỢP — dạng bảng báo cáo       -->
            <!-- ═══════════════════════════════════════════════════ -->
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Bảng: Theo loại VB -->
                <materialDesign:Card Grid.Column="0" Padding="20">
                    <StackPanel>
                        <TextBlock Text="PHÂN LOẠI VĂN BẢN TRONG KỲ" FontSize="12" FontWeight="Bold" Foreground="#37474F" Margin="0,0,0,10"/>
                        <DataGrid x:Name="dgByType" AutoGenerateColumns="False" 
                                  IsReadOnly="True" CanUserAddRows="False"
                                  GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#E0E0E0"
                                  RowHeight="32" HeadersVisibility="Column" MaxHeight="350">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Loại văn bản" Binding="{Binding TypeName}" Width="*">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Padding" Value="4,0"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Đến" Binding="{Binding Den}" Width="55">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                            <Setter Property="Foreground" Value="#E65100"/>
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Đi" Binding="{Binding Di}" Width="55">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                            <Setter Property="Foreground" Value="#2E7D32"/>
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="NB" Binding="{Binding NoiBo}" Width="55">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                            <Setter Property="Foreground" Value="#6A1B9A"/>
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Tổng" Binding="{Binding Total}" Width="55">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </materialDesign:Card>
                
                <!-- Bảng: Theo mức độ khẩn + độ mật -->
                <materialDesign:Card Grid.Column="2" Padding="20">
                    <StackPanel>
                        <TextBlock Text="MỨC ĐỘ KHẨN" FontSize="12" FontWeight="Bold" Foreground="#37474F" Margin="0,0,0,10"/>
                        <DataGrid x:Name="dgByUrgency" AutoGenerateColumns="False" 
                                  IsReadOnly="True" CanUserAddRows="False"
                                  GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#E0E0E0"
                                  RowHeight="32" HeadersVisibility="Column" MaxHeight="200">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Mức độ" Binding="{Binding Label}" Width="*">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Padding" Value="4,0"/>
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Số lượng" Binding="{Binding Count}" Width="80">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Tỷ lệ" Binding="{Binding Percent}" Width="70">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                            <Setter Property="Foreground" Value="#666"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                        
                        <Separator Margin="0,14"/>
                        
                        <TextBlock Text="ĐỘ MẬT" FontSize="12" FontWeight="Bold" Foreground="#37474F" Margin="0,0,0,8"/>
                        <DataGrid x:Name="dgBySecurity" AutoGenerateColumns="False" 
                                  IsReadOnly="True" CanUserAddRows="False"
                                  GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#E0E0E0"
                                  RowHeight="32" HeadersVisibility="Column" MaxHeight="180">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Độ mật" Binding="{Binding Label}" Width="*">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Padding" Value="4,0"/>
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Số lượng" Binding="{Binding Count}" Width="80">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Tỷ lệ" Binding="{Binding Percent}" Width="70">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                            <Setter Property="Foreground" Value="#666"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </materialDesign:Card>
            </Grid>
            
            <!-- ═══════════════════════════════════════════════════ -->
            <!-- PHẦN 4: BẢNG XU HƯỚNG 12 THÁNG                   -->
            <!-- ═══════════════════════════════════════════════════ -->
            <materialDesign:Card Padding="20" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="📈 DIỄN BIẾN 12 THÁNG GẦN NHẤT" FontSize="12" FontWeight="Bold" Foreground="#37474F" Margin="0,0,0,10"/>
                    <DataGrid x:Name="dgMonthlyTrend" AutoGenerateColumns="False" 
                              IsReadOnly="True" CanUserAddRows="False"
                              GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#E0E0E0"
                              RowHeight="34" HeadersVisibility="Column">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Tháng" Binding="{Binding Month}" Width="100">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Setter Property="Padding" Value="4,0"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="VB Đến" Binding="{Binding Den}" Width="80">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Setter Property="Foreground" Value="#E65100"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="VB Đi" Binding="{Binding Di}" Width="80">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Setter Property="Foreground" Value="#2E7D32"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Nội bộ" Binding="{Binding NoiBo}" Width="80">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Setter Property="Foreground" Value="#6A1B9A"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Tổng" Binding="{Binding Total}" Width="80">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="Foreground" Value="#1565C0"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="So với kỳ trước" Binding="{Binding DeltaText}" Width="110">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </materialDesign:Card>
            
        </StackPanel>
    </ScrollViewer>
</Page>
