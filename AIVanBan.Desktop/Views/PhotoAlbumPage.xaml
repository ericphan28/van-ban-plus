<Page x:Class="AIVanBan.Desktop.Views.PhotoAlbumPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      Title="Album ảnh">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="📸 ALBUM ẢNH" 
                   FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Toolbar -->
        <materialDesign:Card Grid.Row="1" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Album Selection -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="Album:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="Bold"/>
                    <ComboBox x:Name="cboAlbum" 
                             Width="200" 
                             SelectionChanged="Album_Changed"/>
                    <Button Style="{StaticResource MaterialDesignIconButton}"
                            ToolTip="Tạo album mới"
                            Click="AddAlbum_Click"
                            Margin="5,0,0,0">
                        <materialDesign:PackIcon Kind="FolderPlus"/>
                    </Button>
                </StackPanel>

                <!-- Actions -->
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Style="{StaticResource MaterialDesignRaisedButton}"
                            Click="UploadPhotos_Click" Margin="0,0,10,0">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Upload" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <TextBlock Text="Tải ảnh lên"/>
                        </StackPanel>
                    </Button>
                    
                    <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                            Click="DeleteSelected_Click" Margin="0,0,10,0">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Delete" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <TextBlock Text="Xóa"/>
                        </StackPanel>
                    </Button>
                    
                    <Button Style="{StaticResource MaterialDesignRaisedSecondaryButton}"
                            Click="Refresh_Click">
                        <materialDesign:PackIcon Kind="Refresh"/>
                    </Button>
                </StackPanel>
            </Grid>
        </materialDesign:Card>

        <!-- Photo Grid -->
        <materialDesign:Card Grid.Row="2">
            <Grid>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <WrapPanel x:Name="photoPanel" Margin="10"/>
                </ScrollViewer>
                
                <!-- Empty State -->
                <StackPanel x:Name="emptyState" 
                           VerticalAlignment="Center" 
                           HorizontalAlignment="Center"
                           Visibility="Collapsed">
                    <materialDesign:PackIcon Kind="ImageOff" Width="80" Height="80" 
                                            Foreground="Gray" HorizontalAlignment="Center"/>
                    <TextBlock Text="Chưa có ảnh" FontSize="18" Foreground="Gray" 
                              Margin="0,10,0,0" HorizontalAlignment="Center"/>
                    <TextBlock Text="Click 'Tải ảnh lên' để thêm ảnh" 
                              Foreground="Gray" HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </materialDesign:Card>
    </Grid>
</Page>
