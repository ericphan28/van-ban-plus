<Window x:Class="AIVanBan.Desktop.Views.PhotoImportPreviewDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Chọn ảnh để import" 
        Height="700" Width="1000"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <materialDesign:Card Grid.Row="0" Margin="10" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Chọn ảnh để import vào album" 
                               FontSize="18" 
                               FontWeight="Bold"/>
                    <TextBlock x:Name="txtStats" 
                               Text="Đã chọn: 0 / 0 ảnh (0 MB)" 
                               FontSize="12" 
                               Foreground="#666"
                               Margin="0,5,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="✅ Chọn tất cả"
                            Click="SelectAll_Click"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            Margin="0,0,10,0"
                            Padding="10,5"/>
                    
                    <Button Content="❌ Bỏ chọn tất cả"
                            Click="DeselectAll_Click"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            Padding="10,5"/>
                </StackPanel>
            </Grid>
        </materialDesign:Card>

        <!-- Photos Grid with Checkboxes -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="10">
            <ItemsControl x:Name="photosPanel">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <materialDesign:Card Width="220" Margin="10" Padding="10">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="200"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Photo Preview -->
                                <Border Grid.Row="0" 
                                        Background="#F0F0F0" 
                                        BorderBrush="#DDD" 
                                        BorderThickness="1"
                                        CornerRadius="4">
                                    <Grid>
                                        <Image Source="{Binding ImageSource}" 
                                               Stretch="UniformToFill"
                                               StretchDirection="Both"/>
                                        
                                        <!-- Checkbox Overlay -->
                                        <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                                  HorizontalAlignment="Right"
                                                  VerticalAlignment="Top"
                                                  Margin="10"
                                                  Background="White"
                                                  Checked="Photo_CheckChanged"
                                                  Unchecked="Photo_CheckChanged"/>
                                    </Grid>
                                </Border>

                                <!-- Photo Info -->
                                <StackPanel Grid.Row="1" Margin="0,10,0,0">
                                    <TextBlock Text="{Binding FileName}" 
                                               FontWeight="SemiBold"
                                               FontSize="12"
                                               TextTrimming="CharacterEllipsis"
                                               ToolTip="{Binding FileName}"/>
                                    <TextBlock Text="{Binding DisplayInfo}" 
                                               FontSize="10"
                                               Foreground="#666"
                                               Margin="0,3,0,0"/>
                                </StackPanel>
                            </Grid>
                        </materialDesign:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Footer Actions -->
        <materialDesign:Card Grid.Row="2" Margin="10" Padding="15">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="❌ Hủy"
                            Click="Cancel_Click"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            Margin="0,0,10,0"
                            Padding="20,8"/>
                    
                    <Button Content="✅ Import ảnh đã chọn"
                            x:Name="btnImport"
                            Click="Import_Click"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Background="#4CAF50"
                            BorderBrush="#4CAF50"
                            Padding="20,8"/>
                </StackPanel>
            </Grid>
        </materialDesign:Card>
    </Grid>
</Window>
