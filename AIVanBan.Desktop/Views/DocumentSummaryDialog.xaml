<Window x:Class="AIVanBan.Desktop.Views.DocumentSummaryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="📝 AI Tóm tắt Văn bản" 
        Width="900" Height="700"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ═══ HEADER ═══ -->
        <Border Grid.Row="0" Background="#6A1B9A" Padding="20,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="📝 AI TÓM TẮT VĂN BẢN" 
                              FontSize="18" FontWeight="Bold" Foreground="White"/>
                    <TextBlock x:Name="txtHeaderInfo" 
                              Text="Phân tích • Tóm tắt • Trích xuất nội dung chính" 
                              FontSize="12" Foreground="#CE93D8" Margin="0,4,0,0"/>
                </StackPanel>
                <!-- Document type badge -->
                <Border x:Name="badgeDocType" Grid.Column="1" 
                       Background="#4A148C" CornerRadius="8" Padding="14,6" 
                       VerticalAlignment="Center" Visibility="Collapsed">
                    <TextBlock x:Name="txtDocTypeBadge" FontSize="12" 
                              FontWeight="Bold" Foreground="White"/>
                </Border>
            </Grid>
        </Border>

        <!-- ═══ QUICK INPUT PANEL ═══ -->
        <Border x:Name="pnlQuickInput" Grid.Row="1" Background="White" 
               Padding="16,12" Visibility="Collapsed"
               BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0" Text="📝 Xem lại hoặc dán nội dung văn bản cần tóm tắt:" 
                          FontSize="13" FontWeight="SemiBold" Foreground="#333" Margin="0,0,0,8"/>
                
                <TextBox x:Name="txtQuickInput" Grid.Row="1"
                        TextWrapping="Wrap" AcceptsReturn="True"
                        VerticalScrollBarVisibility="Auto"
                        Height="160" FontSize="13"
                        materialDesign:HintAssist.Hint="Paste nội dung văn bản vào đây..."
                        Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                
                <Grid Grid.Row="2" Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock x:Name="txtCharCount" Grid.Column="0" 
                              Text="0 ký tự" FontSize="11" Foreground="#999"
                              VerticalAlignment="Center"/>
                    
                    <ComboBox x:Name="cboQuickDocType" Grid.Column="1" 
                             Width="160" Height="36" Margin="0,0,10,0"
                             materialDesign:HintAssist.Hint="Loại văn bản (tùy chọn)"
                             materialDesign:HintAssist.IsFloating="True"
                             FontSize="12"/>
                    
                    <Button x:Name="btnQuickAction" Grid.Column="2"
                           Style="{StaticResource MaterialDesignRaisedButton}"
                           Background="#6A1B9A" Foreground="White"
                           Height="36" Padding="20,0"
                           Click="QuickAction_Click">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="TextBoxCheck" Width="18" Height="18" 
                                                    VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <TextBlock Text="Tóm tắt" FontSize="13" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </Grid>
            </Grid>
        </Border>

        <!-- ═══ MAIN CONTENT ═══ -->
        <Grid Grid.Row="2">
            
            <!-- Loading state -->
            <StackPanel x:Name="pnlLoading" VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="6" 
                           Style="{StaticResource MaterialDesignLinearProgressBar}"/>
                <TextBlock x:Name="txtLoadingStatus" 
                          Text="🤖 Đang phân tích văn bản..." 
                          FontSize="14" HorizontalAlignment="Center" Margin="0,16,0,0"
                          Foreground="#666"/>
                <TextBlock x:Name="txtLoadingTimer" Text="" 
                          FontSize="12" HorizontalAlignment="Center" Margin="0,6,0,0"
                          Foreground="#999"/>
            </StackPanel>

            <!-- Result content -->
            <ScrollViewer x:Name="pnlResults" VerticalScrollBarVisibility="Auto" 
                         Padding="16" Visibility="Collapsed">
                <StackPanel>

                    <!-- Card 1: Tóm tắt ngắn gọn -->
                    <materialDesign:Card Margin="0,0,0,12" Padding="16">
                        <StackPanel>
                            <TextBlock Text="📋 TÓM TẮT NGẮN GỌN" 
                                      FontWeight="Bold" FontSize="13" Foreground="#6A1B9A" Margin="0,0,0,8"/>
                            <Border Background="#F3E5F5" CornerRadius="8" Padding="14,10">
                                <TextBlock x:Name="txtBrief" TextWrapping="Wrap" 
                                          FontSize="14" LineHeight="24" Foreground="#4A148C"
                                          FontWeight="SemiBold"/>
                            </Border>
                        </StackPanel>
                    </materialDesign:Card>

                    <!-- Card 2: Thông tin văn bản (Type, Issuer, Audience) -->
                    <materialDesign:Card Margin="0,0,0,12" Padding="16">
                        <StackPanel>
                            <TextBlock Text="📄 THÔNG TIN VĂN BẢN" 
                                      FontWeight="Bold" FontSize="13" Foreground="#6A1B9A" Margin="0,0,0,10"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- Loại VB -->
                                <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                    <TextBlock Text="Loại văn bản" FontSize="11" 
                                              Foreground="#888" Margin="0,0,0,4"/>
                                    <Border Background="#EDE7F6" CornerRadius="6" Padding="8,6">
                                        <TextBlock x:Name="txtDocType" TextWrapping="Wrap" 
                                                  FontSize="13" FontWeight="SemiBold" Foreground="#4A148C"/>
                                    </Border>
                                </StackPanel>
                                
                                <!-- Cơ quan ban hành -->
                                <StackPanel Grid.Column="1" Margin="4,0,4,0">
                                    <TextBlock Text="Cơ quan ban hành" FontSize="11" 
                                              Foreground="#888" Margin="0,0,0,4"/>
                                    <Border Background="#E3F2FD" CornerRadius="6" Padding="8,6">
                                        <TextBlock x:Name="txtIssuingAuth" TextWrapping="Wrap" 
                                                  FontSize="13" FontWeight="SemiBold" Foreground="#1565C0"/>
                                    </Border>
                                </StackPanel>
                                
                                <!-- Đối tượng áp dụng -->
                                <StackPanel Grid.Column="2" Margin="8,0,0,0">
                                    <TextBlock Text="Đối tượng áp dụng" FontSize="11" 
                                              Foreground="#888" Margin="0,0,0,4"/>
                                    <Border Background="#E8F5E9" CornerRadius="6" Padding="8,6">
                                        <TextBlock x:Name="txtTargetAudience" TextWrapping="Wrap" 
                                                  FontSize="13" FontWeight="SemiBold" Foreground="#2E7D32"/>
                                    </Border>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </materialDesign:Card>

                    <!-- Card 3: Nội dung chính (Key Points) -->
                    <materialDesign:Card Margin="0,0,0,12" Padding="16">
                        <StackPanel>
                            <TextBlock Text="📌 NỘI DUNG CHÍNH" 
                                      FontWeight="Bold" FontSize="13" Foreground="#6A1B9A" Margin="0,0,0,8"/>
                            <ItemsControl x:Name="lstKeyPoints">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#F3E5F5" CornerRadius="8" 
                                               Padding="14,10" Margin="0,0,0,8">
                                            <StackPanel>
                                                <TextBlock Text="{Binding Heading}" 
                                                          FontWeight="Bold" FontSize="13" 
                                                          Foreground="#6A1B9A" Margin="0,0,0,4"/>
                                                <TextBlock Text="{Binding Content}" 
                                                          TextWrapping="Wrap" FontSize="13" 
                                                          LineHeight="22" Foreground="#333"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </materialDesign:Card>

                    <!-- Card 4: Căn cứ pháp lý -->
                    <materialDesign:Card x:Name="cardLegalBases" Margin="0,0,0,12" Padding="16"
                                        Visibility="Collapsed">
                        <StackPanel>
                            <TextBlock Text="⚖️ CĂN CỨ PHÁP LÝ" 
                                      FontWeight="Bold" FontSize="13" Foreground="#6A1B9A" Margin="0,0,0,8"/>
                            <ItemsControl x:Name="lstLegalBases">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#EDE7F6" CornerRadius="6" 
                                               Padding="10,6" Margin="0,0,0,4">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" Text="📜" FontSize="13" 
                                                          Margin="0,0,8,0" VerticalAlignment="Top"/>
                                                <TextBlock Grid.Column="1" Text="{Binding}" TextWrapping="Wrap" 
                                                          FontSize="12.5" Foreground="#4A148C"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </materialDesign:Card>

                    <!-- Card 5: Mốc thời gian -->
                    <materialDesign:Card x:Name="cardDates" Margin="0,0,0,12" Padding="16"
                                        Visibility="Collapsed">
                        <StackPanel>
                            <TextBlock Text="📅 CÁC MỐC THỜI GIAN" 
                                      FontWeight="Bold" FontSize="13" Foreground="#E65100" Margin="0,0,0,8"/>
                            <ItemsControl x:Name="lstDates">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#FFF3E0" CornerRadius="6" 
                                               Padding="10,6" Margin="0,0,0,4">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" Text="⏰" FontSize="13" 
                                                          Margin="0,0,8,0" VerticalAlignment="Top"/>
                                                <TextBlock Grid.Column="1" Text="{Binding}" TextWrapping="Wrap" 
                                                          FontSize="12.5" Foreground="#E65100"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </materialDesign:Card>

                    <!-- Card 6: Số liệu quan trọng -->
                    <materialDesign:Card x:Name="cardFigures" Margin="0,0,0,12" Padding="16"
                                        Visibility="Collapsed">
                        <StackPanel>
                            <TextBlock Text="📊 SỐ LIỆU QUAN TRỌNG" 
                                      FontWeight="Bold" FontSize="13" Foreground="#1565C0" Margin="0,0,0,8"/>
                            <ItemsControl x:Name="lstFigures">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#E3F2FD" CornerRadius="6" 
                                               Padding="10,6" Margin="0,0,0,4">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" Text="🔢" FontSize="13" 
                                                          Margin="0,0,8,0" VerticalAlignment="Top"/>
                                                <TextBlock Grid.Column="1" Text="{Binding}" TextWrapping="Wrap" 
                                                          FontSize="12.5" FontWeight="SemiBold" Foreground="#1565C0"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </materialDesign:Card>

                    <!-- Card 7: Tác động / Ý nghĩa -->
                    <materialDesign:Card x:Name="cardImpact" Margin="0,0,0,12" Padding="16"
                                        Visibility="Collapsed">
                        <StackPanel>
                            <TextBlock Text="💡 TÁC ĐỘNG / Ý NGHĨA" 
                                      FontWeight="Bold" FontSize="13" Foreground="#00796B" Margin="0,0,0,8"/>
                            <Border Background="#E0F2F1" CornerRadius="6" Padding="12,8">
                                <TextBlock x:Name="txtImpact" TextWrapping="Wrap" 
                                          FontSize="13" LineHeight="22" Foreground="#004D40"/>
                            </Border>
                        </StackPanel>
                    </materialDesign:Card>

                    <!-- Card 8: Lưu ý đặc biệt -->
                    <materialDesign:Card x:Name="cardNotes" Margin="0,0,0,12" Padding="16"
                                        Visibility="Collapsed">
                        <Border Background="#FFF8E1" CornerRadius="6" Padding="12,10">
                            <StackPanel>
                                <TextBlock Text="⚠️ LƯU Ý ĐẶC BIỆT" 
                                          FontWeight="Bold" FontSize="13" Foreground="#F57F17" Margin="0,0,0,6"/>
                                <TextBlock x:Name="txtNotes" TextWrapping="Wrap" 
                                          FontSize="13" LineHeight="20" Foreground="#E65100"/>
                            </StackPanel>
                        </Border>
                    </materialDesign:Card>

                </StackPanel>
            </ScrollViewer>

            <!-- Error state -->
            <StackPanel x:Name="pnlError" VerticalAlignment="Center" HorizontalAlignment="Center"
                       Visibility="Collapsed">
                <TextBlock Text="❌" FontSize="48" HorizontalAlignment="Center"/>
                <TextBlock x:Name="txtError" Text="" TextWrapping="Wrap" MaxWidth="500"
                          FontSize="14" HorizontalAlignment="Center" Margin="0,12,0,0"
                          Foreground="#D32F2F" TextAlignment="Center"/>
                <Button Content="🔄 Thử lại" Margin="0,16,0,0" 
                       Style="{StaticResource MaterialDesignRaisedButton}"
                       Background="#6A1B9A" Foreground="White"
                       Click="Retry_Click"/>
            </StackPanel>
        </Grid>

        <!-- ═══ FOOTER ═══ -->
        <Border Grid.Row="3" Background="White" BorderBrush="#E0E0E0" 
               BorderThickness="0,1,0,0" Padding="16,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="txtFooterInfo" Grid.Column="0" 
                          VerticalAlignment="Center" FontSize="12" Foreground="#888"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="btnCopy" Content="📋 Copy tóm tắt"
                           Style="{StaticResource MaterialDesignOutlinedButton}"
                           Click="CopySummary_Click"
                           Margin="0,0,8,0" Visibility="Collapsed"/>
                    <Button Content="Đóng"
                           Style="{StaticResource MaterialDesignOutlinedButton}"
                           Click="Close_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
