<Window x:Class="AIVanBan.Desktop.Views.PeriodicReportDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="📊 AI Soạn báo cáo định kỳ" 
        Height="780" 
        Width="1050"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        FontFamily="{DynamicResource MaterialDesignFont}">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="📊 AI Soạn báo cáo định kỳ" 
                      Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                      Margin="0,0,0,5"/>
            <TextBlock Text="Nhập số liệu thô → AI tự tính toán, so sánh kỳ trước, viết đánh giá + kiến nghị"
                      Style="{StaticResource MaterialDesignBody2TextBlock}"
                      Foreground="{DynamicResource MaterialDesignBodyLight}"/>
        </StackPanel>

        <!-- Main Content: 2 columns -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- LEFT: Input Form -->
            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <!-- Kịch bản mẫu (demo) - đặt trên cùng cho dễ thấy -->
                    <materialDesign:Card Padding="16" Margin="0,0,0,12"
                                         Background="#FFF3E0">
                        <StackPanel>
                            <TextBlock Text="🎯 Dữ liệu demo mẫu" 
                                      FontWeight="Bold" FontSize="14" 
                                      Foreground="#E65100"
                                      Margin="0,0,0,4"/>
                            <TextBlock Text="Chọn kịch bản → Tải → Bấm Tạo báo cáo bên phải"
                                      FontSize="12" Foreground="#BF360C"
                                      Margin="0,0,0,8"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ComboBox x:Name="cboSampleScenario"
                                         materialDesign:HintAssist.Hint="Chọn kịch bản"
                                         Style="{StaticResource MaterialDesignOutlinedComboBox}"
                                         FontSize="12"/>
                                <Button Grid.Column="2"
                                       Style="{StaticResource MaterialDesignFlatMidBgButton}"
                                       Content="📝 Tải demo"
                                       Click="LoadSample_Click"
                                       Padding="16,6"
                                       FontWeight="Bold"/>
                            </Grid>
                        </StackPanel>
                    </materialDesign:Card>

                    <!-- Import từ file Word (tháng → quý) -->
                    <materialDesign:Card Padding="16" Margin="0,0,0,12"
                                         Background="#E3F2FD">
                        <StackPanel>
                            <TextBlock Text="📂 Nhập từ file Word (tổng hợp tháng → quý)" 
                                      FontWeight="Bold" FontSize="14" 
                                      Foreground="#0D47A1"
                                      Margin="0,0,0,4"/>
                            <TextBlock Text="Chọn nhiều file BC tháng (.docx) → tự trích xuất nội dung + bảng biểu → AI tổng hợp thành BC quý"
                                      FontSize="11" Foreground="#1565C0" TextWrapping="Wrap"
                                      Margin="0,0,0,8"/>
                            <Button x:Name="btnImportWordFiles"
                                   Style="{StaticResource MaterialDesignRaisedButton}"
                                   Background="#1565C0" Foreground="White"
                                   Click="ImportWordFiles_Click"
                                   HorizontalAlignment="Stretch"
                                   Padding="12,8">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="FileWordBox" Width="20" Height="20" Margin="0,0,8,0"/>
                                    <TextBlock Text="📂 Chọn file Word báo cáo tháng..."/>
                                </StackPanel>
                            </Button>
                            <TextBlock x:Name="txtImportedFilesInfo"
                                      Text=""
                                      FontSize="11" Foreground="#2E7D32" TextWrapping="Wrap"
                                      Margin="0,6,0,0"
                                      Visibility="Collapsed"/>
                        </StackPanel>
                    </materialDesign:Card>

                    <!-- Thông tin cơ bản -->
                    <materialDesign:Card Padding="16" Margin="0,0,0,12">
                        <StackPanel>
                            <TextBlock Text="🏛️ Thông tin cơ bản" 
                                      FontWeight="SemiBold" FontSize="14" 
                                      Margin="0,0,0,12"/>

                            <!-- Đơn vị -->
                            <TextBox x:Name="txtOrgName" 
                                     materialDesign:HintAssist.Hint="Tên đơn vị (VD: UBND xã Gia Kiệm)"
                                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                     Margin="0,0,0,12"/>

                            <!-- Loại kỳ -->
                            <ComboBox x:Name="cboPeriodType"
                                     materialDesign:HintAssist.Hint="Loại kỳ báo cáo"
                                     Style="{StaticResource MaterialDesignOutlinedComboBox}"
                                     SelectionChanged="PeriodType_Changed"
                                     Margin="0,0,0,12"/>

                            <!-- Kỳ báo cáo -->
                            <ComboBox x:Name="cboPeriod"
                                     materialDesign:HintAssist.Hint="Kỳ báo cáo"
                                     Style="{StaticResource MaterialDesignOutlinedComboBox}"
                                     IsEditable="True"
                                     Margin="0,0,0,12"/>

                            <!-- Lĩnh vực -->
                            <ComboBox x:Name="cboField"
                                     materialDesign:HintAssist.Hint="Lĩnh vực"
                                     Style="{StaticResource MaterialDesignOutlinedComboBox}"
                                     IsEditable="True"
                                     Margin="0,0,0,12"/>

                            <!-- Người ký + Chức danh -->
                            <Grid Margin="0,0,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="txtSignerName" 
                                         materialDesign:HintAssist.Hint="Người ký"
                                         Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                                <TextBox x:Name="txtSignerTitle" Grid.Column="2"
                                         materialDesign:HintAssist.Hint="Chức danh"
                                         Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                            </Grid>
                        </StackPanel>
                    </materialDesign:Card>

                    <!-- Số liệu -->
                    <materialDesign:Card Padding="16" Margin="0,0,0,12">
                        <StackPanel>
                            <Grid Margin="0,0,0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="📈 Số liệu kỳ hiện tại" 
                                          FontWeight="SemiBold" FontSize="14"/>
                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <Button x:Name="btnImportWordData" 
                                            Style="{StaticResource MaterialDesignOutlinedButton}"
                                            Height="30" FontSize="11" Padding="10,0"
                                            Click="ImportWordToData_Click"
                                            ToolTip="Nhập số liệu từ file Word (.docx) có bảng biểu"
                                            Margin="0,0,6,0">
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Kind="FileWord" Width="16" Height="16" Margin="0,0,4,0"/>
                                            <TextBlock Text="Từ Word"/>
                                        </StackPanel>
                                    </Button>
                                    <Button x:Name="btnAutoFillStats"
                                            Style="{StaticResource MaterialDesignOutlinedButton}"
                                            Height="30" FontSize="11" Padding="10,0"
                                            Click="AutoFillStats_Click"
                                            ToolTip="Tự động trích xuất số liệu từ sổ văn bản trong kỳ báo cáo">
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Kind="DatabaseExport" Width="16" Height="16" Margin="0,0,4,0"/>
                                            <TextBlock Text="Lấy từ sổ VB"/>
                                        </StackPanel>
                                    </Button>
                                </StackPanel>
                            </Grid>
                            <TextBlock Text="Nhập/paste số liệu tự do, hoặc nhấn 'Lấy từ sổ VB' để auto-fill từ hệ thống."
                                      Foreground="{DynamicResource MaterialDesignBodyLight}"
                                      FontSize="12" Margin="0,0,0,8"/>
                            
                            <TextBox x:Name="txtRawData"
                                     materialDesign:HintAssist.Hint="VD: Thu ngân sách: 850 triệu. Hộ nghèo: 45 hộ..."
                                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                     AcceptsReturn="True"
                                     TextWrapping="Wrap"
                                     MinHeight="150"
                                     MaxHeight="250"
                                     VerticalScrollBarVisibility="Auto">
                                <TextBox.ToolTip>
                                    <ToolTip>
                                        <StackPanel MaxWidth="350">
                                            <TextBlock FontWeight="Bold" Margin="0,0,0,5">💡 Mẹo nhập số liệu:</TextBlock>
                                            <TextBlock TextWrapping="Wrap">Nhập tự do, mỗi dòng một chỉ tiêu.</TextBlock>
                                            <TextBlock TextWrapping="Wrap" Margin="0,5,0,0" FontWeight="SemiBold">Ví dụ:</TextBlock>
                                            <TextBlock TextWrapping="Wrap">Thu ngân sách: 850 triệu (KH: 900 triệu)</TextBlock>
                                            <TextBlock TextWrapping="Wrap">Hộ nghèo: 45 hộ (tháng trước: 47)</TextBlock>
                                            <TextBlock TextWrapping="Wrap">Giải quyết TTHC: 312 hồ sơ, trễ hạn: 5</TextBlock>
                                            <TextBlock TextWrapping="Wrap">DVC trực tuyến: 420 hồ sơ</TextBlock>
                                            <TextBlock TextWrapping="Wrap" Margin="0,5,0,0" Foreground="#1565C0">AI sẽ tự tính %, tăng/giảm, tỷ lệ.</TextBlock>
                                        </StackPanel>
                                    </ToolTip>
                                </TextBox.ToolTip>
                            </TextBox>
                        </StackPanel>
                    </materialDesign:Card>

                    <!-- BC kỳ trước -->
                    <materialDesign:Card Padding="16" Margin="0,0,0,12">
                        <StackPanel>
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="📋 Báo cáo kỳ trước (tùy chọn)" 
                                          FontWeight="SemiBold" FontSize="14"
                                          VerticalAlignment="Center"/>
                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                                           Height="30" FontSize="11" Padding="8,0"
                                           Click="ImportWordToPrevious_Click"
                                           ToolTip="Nhập BC kỳ trước từ file Word"
                                           Margin="0,0,6,0">
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Kind="FileWord" Width="14" Height="14" Margin="0,0,3,0"/>
                                            <TextBlock Text="Word"/>
                                        </StackPanel>
                                    </Button>
                                    <ComboBox x:Name="cboPreviousReport"
                                             materialDesign:HintAssist.Hint="Chọn từ DB"
                                             Style="{StaticResource MaterialDesignOutlinedComboBox}"
                                             Width="140" FontSize="12"
                                             DisplayMemberPath="Title"
                                             SelectionChanged="PreviousReport_Changed"/>
                                </StackPanel>
                            </Grid>
                            <TextBlock Text="Paste nội dung BC kỳ trước để AI so sánh tự động."
                                      Foreground="{DynamicResource MaterialDesignBodyLight}"
                                      FontSize="12" Margin="0,0,0,8"/>
                            
                            <TextBox x:Name="txtPreviousReport"
                                     materialDesign:HintAssist.Hint="Nội dung báo cáo kỳ trước (hoặc chọn từ DB ở trên)"
                                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                     AcceptsReturn="True"
                                     TextWrapping="Wrap"
                                     MinHeight="100"
                                     MaxHeight="200"
                                     VerticalScrollBarVisibility="Auto"/>
                        </StackPanel>
                    </materialDesign:Card>


                </StackPanel>
            </ScrollViewer>

            <!-- RIGHT: Preview / Result -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Title + Generate button -->
                <Grid Grid.Row="0" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="📄 Kết quả báo cáo" 
                              FontWeight="SemiBold" FontSize="14"
                              VerticalAlignment="Center"/>

                    <Button x:Name="btnGenerate" Grid.Column="1"
                           Style="{StaticResource MaterialDesignRaisedButton}"
                           Background="#00796B" Foreground="White"
                           Click="Generate_Click">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Creation" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <TextBlock Text="🤖 Tạo báo cáo"/>
                        </StackPanel>
                    </Button>
                </Grid>

                <!-- Loading -->
                <StackPanel x:Name="LoadingPanel" Grid.Row="1" 
                           VerticalAlignment="Center" HorizontalAlignment="Center"
                           Visibility="Collapsed">
                    <ProgressBar IsIndeterminate="True" Width="200" Margin="0,0,0,12"/>
                    <TextBlock x:Name="txtLoading" Text="⏳ AI đang phân tích số liệu và soạn báo cáo..."
                              HorizontalAlignment="Center"
                              Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                </StackPanel>

                <!-- Placeholder -->
                <StackPanel x:Name="PlaceholderPanel" Grid.Row="1" 
                           VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="📊" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,12"/>
                    <TextBlock Text="Nhập thông tin bên trái rồi bấm &quot;Tạo báo cáo&quot;"
                              HorizontalAlignment="Center" FontSize="14"
                              Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                    <TextBlock Text="AI sẽ tự tính toán, so sánh và soạn báo cáo hoàn chỉnh"
                              HorizontalAlignment="Center" FontSize="12" Margin="0,4,0,0"
                              Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                </StackPanel>

                <!-- Result -->
                <materialDesign:Card x:Name="ResultCard" Grid.Row="1" 
                                    Padding="0" Visibility="Collapsed">
                    <RichTextBox x:Name="ResultRichTextBox"
                                BorderThickness="0"
                                IsReadOnly="True"
                                VerticalScrollBarVisibility="Auto"
                                Padding="16"
                                FontFamily="Times New Roman"
                                FontSize="14">
                        <RichTextBox.Document>
                            <FlowDocument/>
                        </RichTextBox.Document>
                    </RichTextBox>
                </materialDesign:Card>

                <!-- Action buttons -->
                <StackPanel x:Name="ActionButtons" Grid.Row="2" 
                           Orientation="Horizontal" HorizontalAlignment="Right"
                           Margin="0,8,0,0"
                           Visibility="Collapsed">
                    <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                           Content="📋 Copy" Click="Copy_Click" Margin="0,0,8,0"/>
                    <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                           Content="💾 Xuất Word" Click="ExportWord_Click" Margin="0,0,8,0"/>
                    <Button x:Name="btnSaveAsDocument"
                           Style="{StaticResource MaterialDesignRaisedButton}"
                           Content="💾 Lưu vào kho VB" Click="SaveAsDocument_Click"/>
                </StackPanel>
            </Grid>
        </Grid>

        <!-- Bottom Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <TextBlock Text="⚠️ Kết quả AI để tham khảo, cần kiểm tra lại trước khi sử dụng."
                      VerticalAlignment="Center" FontSize="11" Margin="0,0,16,0"
                      Foreground="{DynamicResource MaterialDesignBodyLight}"/>
            <Button Style="{StaticResource MaterialDesignFlatButton}"
                   Content="Đóng" Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>
