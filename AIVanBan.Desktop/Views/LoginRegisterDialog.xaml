<Window x:Class="AIVanBan.Desktop.Views.LoginRegisterDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="🔑 Đăng nhập VanBanPlus" Height="520" Width="440"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,20">
            <materialDesign:PackIcon Kind="CloudLock" Width="48" Height="48" 
                                    HorizontalAlignment="Center"
                                    Foreground="{DynamicResource PrimaryHueMidBrush}"/>
            <TextBlock Text="VanBanPlus" FontSize="22" FontWeight="Bold" 
                       TextAlignment="Center" Margin="0,8,0,0"
                       Foreground="{DynamicResource PrimaryHueMidBrush}"/>
            <TextBlock Text="Đăng nhập hoặc tạo tài khoản mới" 
                       FontSize="13" Opacity="0.6" TextAlignment="Center"/>
        </StackPanel>

        <!-- Tab Login / Register -->
        <TabControl x:Name="tabControl" Grid.Row="1" 
                    materialDesign:ColorZoneAssist.Mode="PrimaryLight">
            
            <!-- Tab Đăng nhập -->
            <TabItem Header="🔐 Đăng nhập">
                <StackPanel Margin="0,16,0,0">
                    <TextBox x:Name="txtLoginEmail"
                             materialDesign:HintAssist.Hint="Email"
                             materialDesign:HintAssist.IsFloating="True"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             Margin="0,0,0,12"/>
                    <PasswordBox x:Name="txtLoginPassword"
                                 materialDesign:HintAssist.Hint="Mật khẩu"
                                 materialDesign:HintAssist.IsFloating="True"
                                 Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                                 Margin="0,0,0,16"/>
                    <Button Content="🔐 Đăng nhập" Click="Login_Click"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Height="40" FontSize="14"/>
                    <TextBlock x:Name="txtLoginStatus" TextWrapping="Wrap" 
                               FontSize="12" Margin="0,12,0,0" TextAlignment="Center"/>
                </StackPanel>
            </TabItem>

            <!-- Tab Đăng ký -->
            <TabItem Header="📝 Đăng ký">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="0,16,0,0">
                        <TextBox x:Name="txtRegFullName"
                                 materialDesign:HintAssist.Hint="Họ và tên *"
                                 materialDesign:HintAssist.IsFloating="True"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 Margin="0,0,0,10"/>
                        <TextBox x:Name="txtRegEmail"
                                 materialDesign:HintAssist.Hint="Email *"
                                 materialDesign:HintAssist.IsFloating="True"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 Margin="0,0,0,10"/>
                        <PasswordBox x:Name="txtRegPassword"
                                     materialDesign:HintAssist.Hint="Mật khẩu (≥6 ký tự) *"
                                     materialDesign:HintAssist.IsFloating="True"
                                     Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                                     Margin="0,0,0,10"/>
                        <TextBox x:Name="txtRegPhone"
                                 materialDesign:HintAssist.Hint="Số điện thoại"
                                 materialDesign:HintAssist.IsFloating="True"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 Margin="0,0,0,10"/>
                        <TextBox x:Name="txtRegCompany"
                                 materialDesign:HintAssist.Hint="Cơ quan / Đơn vị"
                                 materialDesign:HintAssist.IsFloating="True"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 Margin="0,0,0,14"/>
                        <Button Content="📝 Tạo tài khoản" Click="Register_Click"
                                Style="{StaticResource MaterialDesignRaisedButton}"
                                Height="40" FontSize="14"/>
                        <TextBlock x:Name="txtRegisterStatus" TextWrapping="Wrap" 
                                   FontSize="12" Margin="0,12,0,0" TextAlignment="Center"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>

        <!-- Footer -->
        <Button Grid.Row="2" Content="Hủy" Click="Cancel_Click"
                Style="{StaticResource MaterialDesignFlatButton}"
                HorizontalAlignment="Center" Margin="0,12,0,0"/>
    </Grid>
</Window>
