<Window x:Class="AIVanBan.Desktop.Views.PhotoViewerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Xem ảnh"
        Width="1000" Height="700"
        WindowStartupLocation="CenterOwner"
        Background="Black"
        WindowState="Maximized"
        KeyDown="Window_KeyDown">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top Bar -->
        <Border Grid.Row="0" Background="#222222" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="txtFileName" 
                           Text="Photo.jpg" 
                           Foreground="White" 
                           FontSize="16" 
                           VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="❌ Xóa ảnh"
                            Click="DeletePhoto_Click"
                            Style="{StaticResource MaterialDesignFlatButton}"
                            Foreground="Red"
                            Margin="0,0,10,0"/>
                    
                    <Button Content="✖ Đóng"
                            Click="Close_Click"
                            Style="{StaticResource MaterialDesignFlatButton}"
                            Foreground="White"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Image Display -->
        <Grid Grid.Row="1">
            <!-- Image -->
            <Image x:Name="imgMain" 
                   Stretch="Uniform"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"/>

            <!-- Navigation Arrows -->
            <Button Content="◀"
                    Click="Previous_Click"
                    Style="{StaticResource MaterialDesignFloatingActionButton}"
                    Background="#88000000"
                    Foreground="White"
                    Width="60"
                    Height="60"
                    FontSize="24"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Margin="20"
                    x:Name="btnPrevious"/>

            <Button Content="▶"
                    Click="Next_Click"
                    Style="{StaticResource MaterialDesignFloatingActionButton}"
                    Background="#88000000"
                    Foreground="White"
                    Width="60"
                    Height="60"
                    FontSize="24"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Margin="20"
                    x:Name="btnNext"/>
        </Grid>

        <!-- Bottom Info Bar -->
        <Border Grid.Row="2" Background="#222222" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock x:Name="txtPhotoIndex" 
                               Text="1 / 10" 
                               Foreground="White" 
                               FontSize="14"
                               VerticalAlignment="Center"
                               Margin="0,0,20,0"/>
                    
                    <TextBlock x:Name="txtPhotoInfo" 
                               Text="1920x1080 • 2.5 MB" 
                               Foreground="#999" 
                               FontSize="12"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <TextBlock Grid.Column="1"
                           Text="💡 Mẹo: Dùng phím ← → để chuyển ảnh, ESC để đóng"
                           Foreground="#666"
                           FontSize="11"
                           HorizontalAlignment="Right"
                           VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</Window>
